
export interface ProductData {
    id: string
    name: string
    type: string
    price: string
    amount: number
    validity: string
};

export interface ProductId {
    id: string
};

export interface Product {
    name: string
    type: string
    price: string
    amount: number
    validity: string
};